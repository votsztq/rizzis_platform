import streamlit as st
import database  # This triggers the SQLite database creation automatically!

st.title("Welcome to the RIZZIS")
st.success("Platform skeleton is operational and connected to the local database file!")
